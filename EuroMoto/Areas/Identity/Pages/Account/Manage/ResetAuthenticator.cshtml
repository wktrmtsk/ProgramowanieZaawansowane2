@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Zresetuj klucz uwierzytelniający.";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Jeśli zresetujesz swój klucz uwierzytelniający, twoja aplikacja uwierzytelniająca nie będzie działać, dopóki jej nie skonfigurujesz na nowo.</strong>
    </p>
    <p>
        Ten proces wyłącza uwierzytelnianie dwuetapowe (2FA) do momentu, aż zweryfikujesz swoją aplikację uwierzytelniającą.
        Jeśli nie ukończysz konfiguracji aplikacji uwierzytelniającej, możesz stracić dostęp do swojego konta.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Zresetuj klucz uwierzytelniający.</button>
    </form>
</div>
